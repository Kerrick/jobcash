# Contributing to Jobcash

    $ bundle install
    $ bundle exec rake dev:init
    $ bundle exec rake dev:server

Open `http://localhost:8925/` to view the app.

Always `bundle exec rake compile` before committing changes.
